// creating a history obj as opposed to reactRouter

import createHistory from 'history/createBrowserHistory';

export default createHistory();

// now we have to create a plainRouter instead of a browserrouter